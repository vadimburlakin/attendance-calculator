# Калькулятор посещаемости студентов

Инструмент командной строки на Node.js для анализа данных о посещаемости студентов и генерации интерактивного HTML-отчета.

## Описание

Это приложение обрабатывает журналы посещаемости студентов и данные о занятиях для расчета:
- Количество посещенных занятий каждым студентом
- Количество пропущенных занятий каждым студентом
- Коэффициент посещаемости (0-100%) для каждого студента

## Получение исходных данных

### Сбор данных из школьной системы

Для получения данных о посещаемости необходимо **перехватить и сохранить XHR-запросы** из панели управления школы.

#### Краткая справка по эндпоинтам

| Эндпоинт | Назначение | Имя файла |
|----------|-----------|-----------|
| `GetStudentEndPointByOverview` | Список студентов | `students_list.txt` |
| `GetJournalDetailsForJournal` | Журнал посещаемости | `attendance_journal.txt` |
| `GetCourseMeetingForJournal` | Информация о занятиях | `[дата]_[дата].txt` |

#### Пошаговая инструкция

1. **Откройте панель управления школы** в браузере
2. **Откройте инструменты разработчика** (F12 или Ctrl+Shift+I)
3. **Перейдите во вкладку "Network" (Сеть)**
4. **Отфильтруйте по XHR-запросам**
5. **Найдите и сохраните ответы от следующих эндпоинтов**

#### Эндпоинты API для сбора данных

Необходимо перехватить три основных эндпоинта:

##### 1. Список студентов
**Эндпоинт**: `/AuthRest/api/jwt/EducationSystem/CourseView/GetStudentEndPointByOverview`

**Содержимое**: Список всех студентов курса
- `studentId` - Уникальный идентификатор студента
- `personFullname` - Полное имя студента

**Сохранить как**: `source_data/students_list.txt`

**Как найти**:
- В панели Network найдите запрос, содержащий `GetStudentEndPointByOverview`
- Щелкните правой кнопкой → "Copy" → "Copy response"
- Вставьте в файл `students_list.txt`

##### 2. Журнал посещаемости
**Эндпоинт**: `/AuthRest/api/jwt/EducationSystem/CourseView/GetJournalDetailsForJournal`

**Содержимое**: Полный журнал посещаемости со всеми записями
- `studentId` - ID студента
- `courseMeetingId` - ID занятия
- `point1Id` - Маркер посещаемости ("0" = присутствовал, "110000148" = отсутствовал)
- `meetingDate` - Дата занятия

**Сохранить как**: `source_data/attendance_journal.txt`

**Как найти**:
- В панели Network найдите запрос, содержащий `GetJournalDetailsForJournal`
- Щелкните правой кнопкой → "Copy" → "Copy response"
- Вставьте в файл `attendance_journal.txt`

##### 3. Информация о занятиях и датах
**Эндпоинт**: `/AuthRest/api/jwt/EducationSystem/CourseView/GetCourseMeetingForJournal`

**Содержимое**: Информация о занятиях курса по диапазонам дат
- `id` - ID занятия (используется в `courseMeetingId`)
- `meetingDate` - Дата занятия
- `lessonType` - Тип занятия (Лекция, Семинар и т.д.)
- `startTime`, `endTime` - Время начала и окончания
- `code` - Код курса

**Сохранить как**: `source_data/[дата-начала]_[дата-конца].txt` (например, `oct-16_oct-27.txt`)

**Как найти**:
- В панели Network найдите запросы, содержащие `GetCourseMeetingForJournal`
- Обычно таких запросов несколько (по одному на каждый диапазон дат)
- Для каждого запроса: щелкните правой кнопкой → "Copy" → "Copy response"
- Сохраните каждый в отдельный файл с именем по диапазону дат

**Примечание**: Если у вас много данных, система может разбивать запросы на несколько диапазонов. Сохраните все запросы `GetCourseMeetingForJournal` в отдельные файлы.

#### Структура папки source_data/

После сбора данных структура должна выглядеть так:

```
source_data/
├── students_list.txt              # GetStudentEndPointByOverview
├── attendance_journal.txt         # GetJournalDetailsForJournal
├── sep-15_sep-18.txt             # GetCourseMeetingForJournal (диапазон 1)
├── sep-19_sep-29.txt             # GetCourseMeetingForJournal (диапазон 2)
├── oct-16_oct-27.txt             # GetCourseMeetingForJournal (диапазон 3)
├── oct-27_nov-03.txt             # GetCourseMeetingForJournal (диапазон 4)
└── ... (другие диапазоны дат)
```

#### Важные замечания

- **Формат файлов**: Все файлы должны быть в формате JSON, но с расширением `.txt`
- **Кодировка**: Убедитесь, что файлы сохранены в UTF-8
- **Имена файлов**: Для файлов занятий используйте формат `месяц-день_месяц-день.txt`
- **Полнота данных**: Убедитесь, что скопировали полный ответ, а не обрезанный
- **Несколько запросов**: Если система загружает данные о занятиях несколькими запросами, сохраните все

### Быстрая проверка

После сохранения проверьте, что:
1. ✅ В `students_list.txt` есть поля `studentId` и `personFullname`
2. ✅ В `attendance_journal.txt` есть поля `point1Id`, `courseMeetingId`, `studentId`
3. ✅ В файлах занятий есть поля `id`, `meetingDate`, `lessonType`
4. ✅ Все файлы содержат валидный JSON (начинаются с `{` и заканчиваются `}`)

## Структура данных

### Анализ формата данных

После сбора данных приложение анализирует следующую структуру:

#### 1. students_list.txt - Список студентов

**Источник**: Эндпоинт `GetStudentEndPointByOverview`

Сопоставляет ID студентов с их именами:

```json
{
  "tbl": [
    {
      "r": [
        {
          "studentId": "2310132932045410268",
          "personFullname": "Flankesov Flankes",
          ...
        }
      ]
    }
  ]
}
```

**Ключевые поля**:
- `studentId` - Уникальный идентификатор студента
- `personFullname` - Полное имя студента

#### 2. attendance_journal.txt - Журнал посещаемости

**Источник**: Эндпоинт `GetJournalDetailsForJournal`

Содержит все записи о посещаемости:

```json
{
  "tbl": [
    {
      "r": [
        {
          "studentId": "2310132932045410268",
          "courseMeetingId": "250910591702124037",
          "point1Id": "0",
          "point1En": "",
          "meetingDate": "2025-12-26",
          ...
        }
      ]
    }
  ]
}
```

**Ключевые поля**:
- `studentId` - Идентификатор студента
- `courseMeetingId` - Ссылка на занятие
- `point1Id` - **Маркер посещаемости**:
  - `"0"` = Присутствовал (посетил занятие)
  - `"110000148"` = Отсутствовал (пропустил)
- `point1En` - Текстовое описание ("Missing (Absent)" для пропусков, пусто для присутствия)
- `meetingDate` - Дата занятия
- `courseId` - Идентификатор курса

#### 3. Файлы занятий - Информация о курсах

**Источник**: Эндпоинт `GetCourseMeetingForJournal`

Файлы с именами по датам (например, `oct-16_oct-27.txt`, `nov-03_nov-10.txt`):

```json
{
  "tbl": [
    {
      "r": [
        {
          "id": "250910591702124037",
          "meetingDate": "2025-10-17",
          "lessonType": "Mühazirə",
          "lessonTypeEn": "Lecture",
          "startTime": "13:30",
          "endTime": "15:05",
          "code": "2025/2026_PY_İPF-B11.3_23M-29",
          ...
        }
      ]
    }
  ]
}
```

**Ключевые поля**:
- `id` - Идентификатор занятия (используется в `courseMeetingId`)
- `meetingDate` - Дата занятия
- `lessonType` - Тип занятия (Лекция, Семинар и т.д.)
- `startTime`, `endTime` - Время начала и окончания
- `code` - Код курса

### Результаты анализа

Приложение определяет посещаемость по полю `point1Id`:
- **"0"** = Студент присутствовал
- **"110000148"** = Студент отсутствовал
- Остальные значения = Не требуется (студент не записан на это занятие)

## Установка

Зависимости не требуются! Инструмент использует только встроенные модули Node.js.

**Требования**:
- Node.js v14+ (рекомендуется v18)

## Использование

### Базовое использование

#### Вариант 1: Прямое выполнение Node.js

```bash
node calculate-attendance.js
```

#### Вариант 2: С nvm (для пользователей fish shell)

```bash
fish -c "nvm use v18 && node calculate-attendance.js"
```

### Опции командной строки

Вы можете указать пользовательские директории для исходных данных и вывода:

```bash
# Пользовательская папка с данными
node calculate-attendance.js --source ./my_data

# Пользовательская папка для отчета
node calculate-attendance.js --output ./my_reports

# Обе пользовательские папки
node calculate-attendance.js --source ./data/december --output ./reports/december

# Показать справку
node calculate-attendance.js --help
```

**Опции:**
- `-s, --source <путь>` - Путь к папке с исходными данными (по умолчанию: `./source_data`)
- `-o, --output <путь>` - Путь к папке для вывода (по умолчанию: `./output`)
- `-h, --help` - Показать справку

## Результат работы

Инструмент генерирует три файла в папке `output/`:

1. **index.html** - Интерактивный отчет о посещаемости
2. **styles.css** - Стили для отчета
3. **script.js** - Интерактивные функции (поиск, сортировка)

Откройте `output/index.html` в браузере для просмотра отчета.

## Возможности

### Интерактивный HTML-отчет в виде таблицы

#### На экране:

- **Формат таблицы**:
  - **Строки**: Студенты (отсортированы по алфавиту по имени)
  - **Столбцы**: Отдельные занятия (отсортированы по дате и времени)
  - **Ячейки**: Статус посещаемости для каждой комбинации студент-занятие
    - ✓ (Зеленый) = Присутствовал
    - ✗ (Красный) = Отсутствовал
    - — (Серый) = Не требуется (студент не записан на это занятие)

- **Информация о студентах**:
  - Имена студентов (из students_list.txt)
  - Количество посещенных занятий
  - Количество пропущенных занятий
  - Всего занятий
  - Процент посещаемости с цветовым кодированием:
    - 🟢 Зеленый (≥80%): Отличная посещаемость
    - 🔵 Синий (60-79%): Хорошая посещаемость
    - 🟡 Желтый (40-59%): Предупреждение
    - 🔴 Красный (<40%): Плохая посещаемость

- **Поиск**: Фильтрация студентов по имени (поиск в реальном времени)

- **Возможности таблицы**:
  - Закрепленная строка заголовка (остается видимой при прокрутке)
  - Закрепленный первый столбец (имена студентов видны при горизонтальной прокрутке)
  - Адаптивный дизайн
  - Эффекты при наведении для лучшей читаемости

#### При печати:

- **Кнопка печати**: Печать одним нажатием
  - Генерирует оптимизированную для печати версию
  - Сохраняет цвета для лучшей читаемости
  - Можно сохранить как PDF
  - Показывает только **сводную таблицу** со статистикой по студентам
  - Скрывает детальную таблицу с занятиями

**Печатная версия включает**:
- Компактный заголовок
- Таблицу со столбцами: №, Имя студента, Всего занятий, Присутствовал, Пропущено, Посещаемость %
- Цветовое кодирование сохраняется
- Умещается на 1-2 страницах

### Сводная статистика

- Общее количество студентов
- Общее количество обработанных записей о посещаемости
- Средний процент посещаемости по всем студентам
- Общее количество занятий

## Пример вывода

```
🎓 Калькулятор посещаемости студентов
=====================================

📁 Папка с данными: /home/v/projects/calculate-nb/source_data
📁 Папка для отчета: /home/v/projects/calculate-nb/output

Загрузка имен студентов...
Загружено 19 уникальных имен студентов
Загрузка информации о занятиях...
Загрузка 13 файлов занятий...
Загружено 179 занятий
Загрузка записей о посещаемости...
Загружено 722 записей о посещаемости

Построение матрицы посещаемости...
Обработано 19 студентов по 84 занятиям

Генерация HTML отчета...
Генерация CSS...
Генерация JavaScript...

✅ Отчет успешно создан!
📁 Папка с выходными данными: /home/v/projects/calculate-nb/output
🌐 Откройте /home/v/projects/calculate-nb/output/index.html в браузере

Сводка:
  Всего студентов: 19
  Всего занятий: 84
  Всего записей: 722
  Средняя посещаемость: 87.26%
```

## Технические детали

- **Язык**: Чистый JavaScript Node.js (ES6+)
- **Зависимости**: Нет (используются только встроенные модули: `fs`, `path`)
- **Формат ввода**: JSON-файлы с расширением `.txt`
- **Формат вывода**: HTML5, CSS3, Vanilla JavaScript
- **Интерфейс**: Полностью на русском языке

### Алгоритм обработки данных

1. **Загрузка данных**:
   - Чтение списка студентов для сопоставления ID с именами
   - Загрузка информации о всех занятиях
   - Чтение журнала посещаемости

2. **Построение матрицы**:
   - Создание двумерной матрицы: студенты × занятия
   - Определение статуса посещаемости по полю `point1Id`
   - Сортировка студентов по алфавиту, занятий по дате

3. **Расчет статистики**:
   - Подсчет посещенных и пропущенных занятий для каждого студента
   - Расчет процента посещаемости
   - Определение цветовой категории

4. **Генерация отчета**:
   - Создание HTML-таблицы с данными
   - Генерация CSS для визуального оформления
   - Добавление JavaScript для интерактивности

## Структура файлов

```
calculate-nb/
├── calculate-attendance.js   # Основное приложение
├── package.json              # Метаданные проекта
├── README.md                 # Эта документация
├── .gitignore                # Игнорируемые Git файлы
├── source_data/              # Папка с исходными данными
│   ├── students_list.txt     # Сопоставление ID студентов с именами
│   ├── attendance_journal.txt # Все записи о посещаемости
│   ├── sep-15_sep-18.txt     # Занятия по диапазонам дат
│   ├── sep-19_sep-29.txt
│   ├── sep-29_oct-06.txt
│   ├── oct-09_oct-16.txt
│   ├── oct-16_oct-27.txt
│   ├── oct-27_nov-03.txt
│   ├── nov-03_nov-10.txt
│   ├── nov-13_nov-11.txt
│   ├── nov-24_nov-28.txt
│   ├── dec-01_dec-08.txt
│   ├── dec-08_dec-18.txt
│   └── dec-19_dec-25.txt
└── output/                   # Сгенерированные файлы
    ├── index.html            # Интерактивный отчет
    ├── styles.css            # Стили
    └── script.js             # Скрипты
```

## Примеры использования

### Использование по умолчанию

```bash
# Использует ./source_data и создает отчет в ./output
node calculate-attendance.js
```

### Обработка данных из разных папок

```bash
# Данные за сентябрь
node calculate-attendance.js --source ./data/september --output ./reports/september

# Данные за октябрь
node calculate-attendance.js --source ./data/october --output ./reports/october
```

### Архивирование отчетов

```bash
# Создание отчета с датой
node calculate-attendance.js --output ./reports/2024-12-26
```

## Устранение неполадок

### Ошибка: "Папка с данными не найдена"

Убедитесь, что:
1. Папка `source_data/` существует
2. В папке есть необходимые файлы: `students_list.txt`, `attendance_journal.txt` и файлы занятий
3. Путь указан правильно (используйте `--source` для указания другой папки)

### Ошибка чтения JSON

Убедитесь, что:
1. Файлы содержат корректный JSON
2. Файлы сохранены в UTF-8
3. XHR-ответы скопированы полностью

### Отсутствуют имена студентов

Проверьте, что:
1. Файл `students_list.txt` присутствует
2. Файл содержит поля `studentId` и `personFullname`
3. ID студентов совпадают с ID в журнале посещаемости

## Планы на будущее

### Планируемые функции

#### 1. Автоматический сбор данных
**Приоритет: Высокий**

Добавление возможности автоматического сбора данных из школьной системы при наличии имени пользователя и пароля.

**Планируемая реализация**:
```bash
node calculate-attendance.js --auto --username user@example.com --password ******
```

**Функционал**:
- Автоматическая авторизация в системе школы
- Программный вызов API эндпоинтов:
  - `GetStudentEndPointByOverview`
  - `GetJournalDetailsForJournal`
  - `GetCourseMeetingForJournal`
- Автоматическое сохранение данных в `source_data/`
- Поддержка сохранения учетных данных (безопасное хранение)
- Периодический автоматический сбор (cron/планировщик)

**Преимущества**:
- Не нужно вручную перехватывать XHR-запросы
- Возможность автоматизации и планирования отчетов
- Упрощение процесса для конечных пользователей

#### 2. Веб-интерфейс для настройки
- Графический интерфейс для ввода учетных данных
- Выбор курсов и диапазонов дат
- Предпросмотр перед генерацией отчета

#### 3. Расширенная аналитика
- Графики посещаемости по времени
- Тренды и прогнозы
- Сравнение между студентами/группами
- Экспорт в Excel/CSV

#### 4. Уведомления
- Email-уведомления при низкой посещаемости
- Автоматическая отправка отчетов администраторам
- Telegram/другие мессенджеры

#### 5. Многокурсовая поддержка
- Обработка нескольких курсов одновременно
- Сводные отчеты по всем курсам
- Фильтрация по курсам

#### 6. Интеграция с системами
- API для интеграции с другими системами
- Webhook для автоматического обновления
- Экспорт в форматы LMS (Learning Management System)

### Вклад в разработку

Проект открыт для вклада сообщества. Если вы хотите помочь в реализации этих функций или предложить свои идеи:

1. Создайте issue с описанием функционала
2. Предложите pull request с реализацией
3. Поделитесь своими идеями и предложениями

### Техническая реализация автоматического сбора

**Предварительные требования**:
- Библиотека для HTTP-запросов (например, axios)
- Управление сессиями и cookies
- Безопасное хранение учетных данных (keyring, environment variables)

**Примерный алгоритм**:
1. Авторизация через JWT
2. Получение токена доступа
3. Последовательные запросы к трем эндпоинтам
4. Сохранение ответов в файлы
5. Запуск обработки данных

**Безопасность**:
- Шифрование сохраненных учетных данных
- Поддержка переменных окружения
- Опция для одноразового ввода (без сохранения)
- Логирование без раскрытия паролей

---

## Лицензия

MIT

---

## Дополнительная информация

Для получения помощи:
```bash
node calculate-attendance.js --help
```

Для получения подробной информации о структуре данных и анализе, смотрите комментарии в коде.

### Текущая версия

**v1.0.0** - Ручной сбор данных через перехват XHR-запросов

### Обратная связь

Если у вас есть предложения по улучшению или вы обнаружили ошибки, создайте issue в репозитории проекта.
